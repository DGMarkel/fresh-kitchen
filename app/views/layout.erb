<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>

    <style>

      .expired {
        color: red;
        text-align: center;
      }

      .warning {
        color: red;
      }

    </style>

  </head>
  <body>

    <% if logged_in? %>
      <h1 style="float:left;margin-bottom:0px;">Fresh Kitchen</h1>
      <header style="width:100%;height:18px;background:lightgray;clear:both;">
        <a href="/users/<%= current_user.slug %>">Home</a>
        <a href="/users/<%= current_user.slug %>/foods/new" style="margin-left:5px;">Add Item</a>
        <a href="/logout" style="float:right;">Sign Out</a>
      </header>
      <p style="float:right;font-size:10px;margin-top:2px;">Currently logged in as: <%= current_user.username %></p>

      <div class="expired">

      <% if !current_user.expired_foods.empty? %>
          <% current_user.expired_foods.each do |food| %>

            <p>*** Your <%= food.name.downcase %> expired today and <%= food.first_pronoun %>
              removed from your inventory. Eat <%= food.second_pronoun %> quick or dump
              <%= food.second_pronoun %> in the trash! ***</p>

              <% food.delete %>

          <% end %>

      <% end %>

    </div>

    <% end %>


    <%= yield %>

  </body>
</html>
