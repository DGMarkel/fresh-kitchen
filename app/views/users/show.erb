<h1>Welcome, <%= current_user.username %></h1>
<h4>Let's take a look at what you've got in your kitchen:</h4>

<% if !current_user.categories.empty? %>
  <% current_user.categories.each do |category| %>
    <p><a href="/users/<%=current_user.username%>/<%=category.slug %>"><%=category.name%></a>: <%= category.foods.count %> item(s)</p>
  <% end %>

<% else %>

<h4>Your kitchen is currently empty.  Time to do some shopping!<h4>

<% end %>

<h4>Add a <a href="/users/<%= current_user.username %>/foods/new">new item</a> to your inventory.</h4>

<form action="/logout" method="get">
    <input type="submit" value="Logout">
</form>
